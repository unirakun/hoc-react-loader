{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --scope hoc-react-loader-examples start --stream",
    "bootstrap": "lerna bootstrap",
    "ci": "lerna run ci --stream --parallel",
    "lint": "lerna run lint --stream --parallel",
    "build": "lerna run build --stream --parallel"
  },
  "devDependencies": {
    "lerna": "^2.5.1"
  }
}
